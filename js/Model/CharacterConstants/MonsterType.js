/**
 * MonsterType constants - Types of monsters in the game
 * JavaScript doesn't have true enums, so we'll use an object with string constants
 */
const MonsterType = Object.freeze({
  OGRE: 'OGRE',
  DIREWOLF: 'DIREWOLF',
  GOBLIN: 'GOBLIN'
});

module.exports = MonsterType; 